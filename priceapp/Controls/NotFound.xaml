<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="priceapp.Controls.NotFound"
             x:Name="Page"
             VerticalOptions="FillAndExpand"
             Margin="15, 0, 15, 0">
    <ContentView.Margin>
        <OnPlatform x:TypeArguments="Thickness">
            <On Platform="iOS" Value="15, 0, 15, 0" />
            <On Platform="Android" Value="8, 0, 8, 0" />
        </OnPlatform>
    </ContentView.Margin>
    <ContentView.Content>
        <StackLayout>
            <Label Style="{StaticResource PageTitle}"
                   Text="Тут пусто :(" 
                   TextColor="{ StaticResource ColorPrimary}"/>
            <Label Style="{StaticResource TextStyle}"
                   HorizontalTextAlignment="Center"
                   Text="Нічого не знайдено в вашемо районі. Спробуйте змінити радіус пошуку або спробуйте в іншому місці" />
        </StackLayout>
    </ContentView.Content>
</ContentView>