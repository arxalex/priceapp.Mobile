<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ui="clr-namespace:priceapp.UI;assembly=priceapp"
             x:Class="priceapp.Views.SearchPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid Padding="15, 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="25" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ImageButton Grid.Column="0"
                             BackgroundColor="Transparent"
                             Clicked="ImageButton_OnClicked">
                    <ImageButton.Source>
                        <FontImageSource FontFamily="MaterialOutlined"
                                         Glyph="&#xe5c4;"
                                         Color="{AppThemeBinding Dark=White, Light=Black}">
                        </FontImageSource>
                    </ImageButton.Source>
                </ImageButton>
                <ui:RdEntryBorderless Grid.Column="1"
                                      x:Name="SearchEctry"
                                      Placeholder="Пошук"
                                      Margin="5,0,0,0"
                                      HeightRequest="53"
                                      Keyboard="Plain"
                                      FontSize="18" />
            </Grid>
            <BoxView HeightRequest="1" Color="#DFF0E0" Margin="0, -7, 0, 0"></BoxView>
            <ListView>
                <ListView.ItemTemplate></ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>